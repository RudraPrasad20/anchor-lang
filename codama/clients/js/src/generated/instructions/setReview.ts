/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { CODAMA_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const SET_REVIEW_DISCRIMINATOR = new Uint8Array([
  33, 163, 254, 96, 122, 49, 136, 173,
]);

export function getSetReviewDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_REVIEW_DISCRIMINATOR);
}

export type SetReviewInstruction<
  TProgram extends string = typeof CODAMA_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountResturantReview extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountResturantReview extends string
        ? WritableAccount<TAccountResturantReview>
        : TAccountResturantReview,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetReviewInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
  stars: number;
};

export type SetReviewInstructionDataArgs = { name: string; stars: number };

export function getSetReviewInstructionDataEncoder(): Encoder<SetReviewInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['stars', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_REVIEW_DISCRIMINATOR })
  );
}

export function getSetReviewInstructionDataDecoder(): Decoder<SetReviewInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['stars', getU8Decoder()],
  ]);
}

export function getSetReviewInstructionDataCodec(): Codec<
  SetReviewInstructionDataArgs,
  SetReviewInstructionData
> {
  return combineCodec(
    getSetReviewInstructionDataEncoder(),
    getSetReviewInstructionDataDecoder()
  );
}

export type SetReviewAsyncInput<
  TAccountUser extends string = string,
  TAccountResturantReview extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  resturantReview?: Address<TAccountResturantReview>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: SetReviewInstructionDataArgs['name'];
  stars: SetReviewInstructionDataArgs['stars'];
};

export async function getSetReviewInstructionAsync<
  TAccountUser extends string,
  TAccountResturantReview extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CODAMA_PROGRAM_ADDRESS,
>(
  input: SetReviewAsyncInput<
    TAccountUser,
    TAccountResturantReview,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SetReviewInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountResturantReview,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CODAMA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    resturantReview: { value: input.resturantReview ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.resturantReview.value) {
    accounts.resturantReview.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 118, 105, 101, 119, 115])
        ),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.resturantReview),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSetReviewInstructionDataEncoder().encode(
      args as SetReviewInstructionDataArgs
    ),
    programAddress,
  } as SetReviewInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountResturantReview,
    TAccountSystemProgram
  >);
}

export type SetReviewInput<
  TAccountUser extends string = string,
  TAccountResturantReview extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  resturantReview: Address<TAccountResturantReview>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: SetReviewInstructionDataArgs['name'];
  stars: SetReviewInstructionDataArgs['stars'];
};

export function getSetReviewInstruction<
  TAccountUser extends string,
  TAccountResturantReview extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CODAMA_PROGRAM_ADDRESS,
>(
  input: SetReviewInput<
    TAccountUser,
    TAccountResturantReview,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): SetReviewInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountResturantReview,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CODAMA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    resturantReview: { value: input.resturantReview ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.resturantReview),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSetReviewInstructionDataEncoder().encode(
      args as SetReviewInstructionDataArgs
    ),
    programAddress,
  } as SetReviewInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountResturantReview,
    TAccountSystemProgram
  >);
}

export type ParsedSetReviewInstruction<
  TProgram extends string = typeof CODAMA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    resturantReview: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: SetReviewInstructionData;
};

export function parseSetReviewInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetReviewInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      resturantReview: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSetReviewInstructionDataDecoder().decode(instruction.data),
  };
}
